// Ambient declaration file (no import/export): everything here is global.
// Global types are otherwise banned — Nil is the sole sanctioned exception; don't add more.

/** The nullish pair the language itself never named. Runtime counterpart: is.nil(). */
type Nil = null|undefined;
